// Middleware is currently not needed for this simple project
// If you need middleware later, you can add it here

export async function middleware(request) {
  // Currently no middleware logic needed
  return
}

export const config = {
  matcher: [],
}